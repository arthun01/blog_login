

<h1>Postagens Públicas</h1>

<%= link_to "Voltar ao Login", unauthenticated_root_path %>

<div class="w-full">
    <div class="flex flex-col sm:flex-row justify-between items-center pb-5 sm:pb-0">
        <ul >
            <% @posts.each do |post| %>
                <li>
                    <strong><%= post.title %></strong><br>
                    <%= post.content%>
                </li>
            <% end %>
        </ul>
    </div>
</div>